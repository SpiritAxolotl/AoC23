const { Day } = require("../global/day.js");
const { test } = require("../global/test.js");

class DayX extends Day {
  constructor(str) {
    super(str);
    for (const line of this.lines) {
      
    }
  }
}

test(x, DayX, [10, 10], false);