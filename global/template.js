const t = require("../global/test.js");

function dayX(str) {
    const answer = [0, 0];
    const lines = str.split("\n");
    for (const line of lines) {
        
    }
    return answer;
}

t.test(x, dayX, [10, 10]);