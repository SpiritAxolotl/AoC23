const t = require("../global/test.js");

function day5(str) {
    const answer = [0, 0];
    const lines = str.split("\n");
    for (const line of lines) {
        
    }
    return answer;
}

t.test(5, day5, [35, 35]);